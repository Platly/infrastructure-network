projects:
  - name: production
    dir: .
workflows:
  production:
    plan:
      steps:
        - init:
          extra_args: ["backend-config=prod/backend.tfvars"]
        - plan
    apply:
      steps:
        - apply
    workflow_configuration:
      on_pull_request_pushed: ["digger plan"]
      on_pull_request_closed: ["digger unlock"]
      on_commit_to_default: ["digger apply"]